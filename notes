---PROJECT 2 NOTES---

write pseudo code for all the steps.

prevent default
on click, select your topic
header collapse
empty
loader pops up
json fetch
append data
loader goes away
fail

javascript needs to be circular.
need to have a function for every step.

need to put css in so header takes up the whole screen and hides the header
for desktop version auto-height
get javascript working by monday

css last
make sure only getting 12 stories.
json fetch longest block of code.
how to get value from a select field
console log to see if it works
then json fetch
see getting the type of data eg art etc
make sure getting 12 stories. with a pic. dont want ones without a pic. 
use the function method. filter.
use filter to filter out stories that dont have a pic. console log to check
only want 12 stories. 
put those 12 elements in an html element<li>
anything styled in same way repeated in shape and position its a list. 
even if it doesnt seem like a list
then flex box everything

media queries - only things that need to be changed:
width, height, font size
anything thats display flex doesnt need to be changed.

to get the text on the bottom
position absolute
bottom: 0

hide
transition to height.

Design Specifications:

The base font size is 16px and it HEX code is #ffffff
The body font family is Open Sans Light
The medium grey colour of the text in the footer is #c2c2c2
Requirements

Functional requirements:

Dynamically fetch story data and populate it in a webpage via the Top Stories API based a user’s selection from a <select> field
Resize the header area (i.e. logo plus <select>) so that it transitions from occupying the entire screen on page load to only auto height after the <select> field is changed (you will need jQuery for this)
Link each returned image to its URL on the NYT website
General requirements:

Your project must:

Contain a single .html file and an external, minified .css file
Adequately reflect the designer’s vision for the website when built out using HTML and CSS
Demonstrate effective organization of the project’s root directory
Have a Git repository initialized in its root directory with an appropriately customized .gitignore file
Be pushed to GitHub (with all of your commits synced to it)
Contain a README.md file describing the project (e.g. technologies used, personal learnings, etc.)
Use Gulp for running build tasks, such as minification, Sass compilation, and error checking

CSS/Sass requirements:

Your project must:

Make appropriate use of classes and IDs as selectors
Use Sass as a preprocessor, with CSS properly compiled and minified
Use the background property where appropriate
Implement a mobile-first responsive layout using flexbox (no floats)
Demonstrate effective use of box model properties - padding, margin, height, width
Demonstrate effective use of CSS properties for altering the visual display of text (e.g. font-family, font-style, text-transform, etc.)
Incorporate custom fonts using @font-face
JS requirements:

Your project must:

Use Ajax to fetch data from the NYT Top Stories endpoint
Only display a maximum of 12 stories in the grid, and only display a story if it has a photo associated with it
Transition users between UI states, starting where no stories have been returned, and then subsequently transitioning where stories populate the grid (by incorporating a loading gif)
RWD requirements:

Your project must:

Be responsively designed using a mobile-first approach
Be optimized for 3 screen sizes: mobile, min-width: 600px, and min-width: 1000px
Additional requirements:

Be sure to test your project completely in all major browsers (Chrome, Firefox, Safari, Opera, IE11/Edge) when you think you’re finished
Stretch goals:

Want to challenge yourself? Your project could also:

Incorporate a <select> field with custom styles applied (you will need a jQuery plugin for this such as Selectric)
Incorporate a combination of jQuery and CSS3-based animation to animate the movement of the header on the <select> change
Use CSS3 transitions to show and hide the article abstract on hover

JQUERY NOTES

html: <p id="message">Welcome to Spots!</p>
js: document.getElementById('message').innerHTML = 'We love dogs!';
putting it in a variable:
var el = document.getElementById('message');
el.innerHTML = 'We love dogs!';
el.className = 'red'; // adds class name of "red" to the element

var el = document.getElementById('message');
el.innerHTML = 'We love dogs!';
el.className = 'red'; // adds class name of "red" to the element

 <p class="intro">Welcome to my page!</p>
To target this in jquery: $('.intro')
to change the color using css: $('.intro').css('color', 'red');

to store in a variable: var $intro = $('.intro');
$intro.css('color', 'red');
$intro.addClass('greeting');

need to add this to connect to the DOM:
$(document).ready(function() {
   // your jQuery code here...
});
// Or more commonly, you'll see the shorthand version:
$(function() {
   // your jQuery code here...
});

We can use the .prepend(), .append(), and .remove() methods to update content:
// Add new a new item to the end of the list:
var newContent = '<li>three</li>';
$('.numbers').append(newContent);
// Change the content of the second list item:
$('.numbers li:nth-child(2)').text('2');
// Remove the first item from the list:
$('.numbers').children(':first').remove();

Note that .prepend() and .append() will add content inside the element's opening and closing tags respectively.

The .before() and .after() methods will add content just before or just after the selected element.

s we have seen, it's very easy to add classes to elements with jQuery using .addClass():

$('ul').addClass('menu');
And we can similarly remove them with .removeClass():

$('ul').removeClass('menu');

We can also target other element attributes using the .attr() and .prop() methods:

$('li').attr('id', 'active');
$('input').prop('disabled', true)
Ostensibly, .attr() and .prop() do very similar things, but .prop() offers a slightly more modern, semantic approach and allows you to work with element attributes that are booleans, like the example above.

.always(function(){

}))

javascript resources:
30 days of javascript - miniprojects you can download takes half an hour each
codewars - compete with a developer to see who can finish first - helps with dev interviews
helps 

-----

 
//From Itunes example 
// .done(function(data){
//    console.log(data);
//    $.each(data.results,function(key,value){
//      $("album-list").append("<li><img 
//     src='"+value.artworkUrl60+"'>"+value.collectionName+"<"/li>");
//    });
//  })

//cleaner version: 
// .done(function(data){ 
//  albumData=data.results
})//end of done function

 });//end of ajax call


//  $.each(data.results,function(key,value){
//      $("articleswImages").append("+ enter values+");
// $(".stories").append("<li><img src='"+value.artworkUrl60+"'>"+value.collectionName+"</li>");

  //end of.each
 
// $(".ajax-loader").show();   

// $('.stories').empty(); //or .remove

// $(".ajax-loader").hide();

//Fail function
// .fail(function() {
//   console.log (fail);
//   $('stories').append('Sorry! There was a problem, please try again.');
// };  //end of fail






//FROM SLIDES
// $('button').on('click', function() {
//    $.ajax({
//       method: 'GET',
//       url: 'https://api.github.com/users/octocat'
//    })
//    .done(function(data) {
//       $('.user-name').append(data.login);
//    })

/*ITUNES EXAMPLE
$("#album-search").on("submit", function(event){

 $.ajax({
   method:"GET", 
   dataType:"jsonp",
   url:"https://itunes.apple.com/search?entity=album&limit=6&term="
   $("artist-name").val().replace("","+")
   })
   
  .done(function(data){
   console.log(data);
   $.each(data.results,function(key,value){
     $("album-list").append("<li><img 
    src='"+value.artworkUrl60+"'>"+value.collectionName+"<"/li>");
   });
 }) //end of done

   .fail(function(){
     console.log("anything");
     ("album-list").append("<li>no results</li>");
   });  //end of fail
}); //end of on function

$('.album-list').empty(); 
*/

//initial variables itunes example
//eg. albumData, albumItems, artistName, itunesUrl
//    $albumList=(."album-list");



-------------

// //connect to DOM
// $(document).ready(function() {

// //on to make button functional, then prevent page from refreshing
//    $(".sections").on('change', function() {
//      event.preventDefault();

// //NYT API//
// var select = $('.sections') .val();
//    var url = 'https://api.nytimes.com/svc/topstories/v2/' + select + '.json';
//       url += '?' + $.param({'api-key': '53dfced34ab7497ca7331f1f197cb367'
//     })
    
// $.ajax({
//  method: 'GET',
//  url: url
 
// })

//  //.done method to tell browser what to do with the request
//  .done(function(data) {
//    console.log (data);
//  }
//    }) //end of on function
// }); //end of doc.ready